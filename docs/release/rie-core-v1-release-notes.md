# RIE Core v1 - Governed PDF Operator Workflow

## Release state

- Document type: `PRE_RELEASE_RELEASE_NOTES_CANDIDATE`
- Product label: `RIE Core v1`
- Package version represented by source: `0.1.0`
- Release mode: `SOURCE_AND_GOVERNANCE_WITHOUT_BINARY_ATTACHMENT`
- Candidate annotated tag: `v0.56.0-rcis-end-to-end-cli-audit-packaging-release-phase`
- Release authorized: `False`
- Release published: `False`

## Summary

RIE Core v1 establishes the governed repository source and documentation boundary for the accepted PDF operator workflow.

This release candidate records source, architecture, governance, evidence, operator-boundary, rollback, and controlled-pilot requirements.

It is a source-and-governance release candidate without binary attachments.

## Included

The candidate release includes:

- final authorized repository source state;
- governed PDF workflow implementation;
- architecture and governance records;
- accepted Gate 11 workflow evidence;
- Gate 12 release identity and merge boundaries;
- artifact provenance records;
- operator handoff and rollback requirements;
- controlled one-PDF RSV pilot boundary;
- source-and-governance handoff documentation.

## Binary attachment disclosure

No wheel or installation bundle is attached.

The release attachment inventory is empty.

The accepted historical RIE wheel identity is:

- `rie-0.1.0-py3-none-any.whl`;
- SHA-256 `7a276511d4bbc4cbdbcba32d459ae8f7cb106f1423832be65945d2f5a8226362`;
- `301685` bytes.

The accepted historical pypdf wheel identity is:

- `pypdf-6.14.2-py3-none-any.whl`;
- SHA-256 `3f07891af76dc002657e04993ab9b4de81de29f9013b9761d0b7968bff12e946`;
- `349514` bytes.

These fingerprints remain historical evidence only.

The original accepted binaries are not in verified current custody and are not release attachments.

## Installation statement

This release candidate makes no installation or installability claim.

Binary installation, source installation, dependency installation, and runtime deployment are outside this release mode.

Any future binary publication or installation workflow requires a separate authorization chain.

## Supported scope

The governed scope is PDF-only.

The release candidate covers repository source and governance for:

- official-source inspection;
- governed PDF ingestion;
- evidence generation;
- knowledge-record generation;
- prompt-candidate generation and export;
- operator audit output;
- failure-preserving controlled operation.

## Excluded scope

The release candidate does not authorize or include:

- JPEG or PNG processing;
- image knowledge extraction;
- OCR feature expansion;
- automatic neighboring-file traversal;
- full RSV asset-library ingestion;
- local AI generator integration;
- production deployment;
- external release attachment publication;
- real RSV asset processing;
- pilot execution.

## Real RSV asset statement

No real RSV PDF, JPEG, or PNG is included.

Release publication does not authorize real RSV asset use.

The first controlled real RSV PDF pilot requires a separate authorization record and exactly one manually selected approved official PDF.

## Governance and safety

The accepted workflow preserves:

- explicit source identity;
- official-source registration boundary;
- deterministic evidence identity where required;
- governed knowledge output;
- prompt candidates as candidates rather than automatic approvals;
- operator audit history;
- stop-on-failure behavior;
- failure evidence;
- no automatic retry;
- no destructive history rewrite.

## Known limitations

- Binary attachment availability is intentionally excluded.
- Installation support is intentionally excluded.
- Current exact RIE wheel custody is not verified.
- Current exact pypdf wheel custody is not verified.
- The final release commit is not yet known.
- The annotated tag has not been created.
- Gate 12 final release authorization has not been published.
- The controlled real-asset pilot has not been authorized or executed.

## Upgrade and compatibility statement

No compatibility expansion beyond the accepted evidence is claimed.

Unreviewed dependency upgrades, unsupported Python versions, alternate packaging formats, and rebuilt artifacts require separate review.

## Rollback and withdrawal

Repository rollback must use a separately reviewed forward or revert commit.

Published tags must remain immutable.

Any release withdrawal must preserve the release identity, tag, notes, governance decisions, and withdrawal record.

## Finalization

These notes become final only after:

- handoff and release-note materialization is committed;
- selected-mode readiness is re-reviewed;
- final Gate 12 authorization is committed and published;
- fast-forward merge is authorized and verified;
- the annotated tag is created, pushed, and independently verified;
- final resolved commit and tag identities are recorded.

Until then, these are pre-release release-notes candidates.
